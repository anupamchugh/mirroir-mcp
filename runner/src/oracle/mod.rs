// ABOUTME: Oracle module — drift detection now, LLM judge profile registry next.
// ABOUTME: Both consume `judge:` step args + scenario response text to produce verdicts.

pub mod drift;
pub mod judge;
